- name: install and run nginx 
  hosts: mongodb
  tasks:
    - name: installing nginx
      ansible.builtin.yum:
        name: nginx
        state: installed
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
