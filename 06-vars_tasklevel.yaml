-   name: variables at task level
    hosts: localhost
    vars:
    - money: 100000
      land: "1000 acres"
    tasks:
    - name: inherit values from vars
      ansible.builtin.debug:
        msg: "MONEY: {{money}}, LAND: {{land}}"

  